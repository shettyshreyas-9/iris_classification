
make_dataset:
  test_split: 0.22
  seed: 24

models:
  # - model_type: logistic_regression
  #   hyperparameters:
  #     C: [1.0, 0.5]
  #     max_iter: [20, 30]
  #     solver: ['lbfgs', 'liblinear']

  - model_type: random_forest
    hyperparameters:
      n_estimators: [111, 50]
      max_depth: [8, 10]
      # min_samples_split: [2, 5]

  - model_type: svm
    hyperparameters:
      C: [1.0, 4.5]
      kernel: ['rbf', 'linear']
      # gamma: ['scale', 'auto']

